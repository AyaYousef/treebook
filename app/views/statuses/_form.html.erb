<%= simple_form_for(@status) do |f| %>
  <% if @status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status.errors.count, "error") %> prohibited this status from being saved:</h2>

      <ul>
      <% @status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <!-- 3shan n5ally el user yzhar bel profile name w n5taro bndeef collection
   w label_method 3shan n5tar yshar b2ay shakl ex full_name, profile_name or first_name .. etc-->
  <%= f.input :user_id, collection: User.all, label_method: :profile_name %>
  <%= f.input :content %><br>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
